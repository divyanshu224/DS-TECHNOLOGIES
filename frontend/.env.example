# Local: leave empty or use localhost (Vite proxy handles /api)
# VITE_API_URL=http://localhost:5000/api

# Production (Vercel / Netlify) — MUST include /api at the end:
# VITE_API_URL=https://YOUR-BACKEND.onrender.com/api
VITE_API_URL=
